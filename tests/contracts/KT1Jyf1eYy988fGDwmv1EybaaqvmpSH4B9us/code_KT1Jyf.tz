parameter (pair int string);
storage (pair :storage (int %age) (pair (string %name) (address %caddress)));
code { DUP ;
       DIP { CDR @storage_slash_1 } ;
       CAR @_ages_names_slash_2 ;
       SWAP ;
       DUP ;
       CAR %age ;
       SWAP ;
       CDR ;
       CDR %caddress ;
       { DIP { { DIP { DUP } ; SWAP } } ; SWAP } ;
       CDR @names ;
       PAIR %name %caddress ;
       SWAP ;
       PAIR @storage %age ;
       CDR ;
       SWAP ;
       CAR @ages ;
       PAIR @storage %age ;
       DUP ;
       CAR %age ;
       SWAP ;
       CDR ;
       CAR %name ;
       SELF ;
       ADDRESS @addr ;
       SWAP ;
       PAIR %name %caddress ;
       SWAP ;
       PAIR @storage %age ;
       NIL operation ;
       PAIR }