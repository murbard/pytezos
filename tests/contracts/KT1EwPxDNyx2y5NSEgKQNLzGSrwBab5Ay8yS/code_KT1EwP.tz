parameter (pair (contract :Callee (pair int string)) (pair int string));
storage (pair :storage (int %age) (string %name));
code { DUP ;
       DIP { CDR @storage_slash_1 } ;
       CAR @_addr_ages_names_slash_2 ;
       DUP ;
       CAR @addr ;
       PUSH mutez 0 ;
       { DIP { { DIP { DUP } ; SWAP } } ; SWAP } ;
       { CDR ; CDR @names } ;
       { DIP { { DIP { { DIP { DUP } ; SWAP } } ; SWAP } } ; SWAP } ;
       { CDR ; CAR @ages } ;
       PAIR ;
       TRANSFER_TOKENS @op2 ;
       SWAP ;
       DROP ;
       SWAP ;
       NIL operation ;
       { DIP { { DIP { DUP } ; SWAP } } ; SWAP } ;
       DIP { DIP { DIP { DROP } } } ;
       CONS ;
       PAIR }