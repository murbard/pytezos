parameter unit ;
storage (pair
          (map %accounts address (pair (map %allowances address nat) (nat %balance)))
          (nat %s));
code {
    CDR ;
    CAR ;
    MAP { CDDR } ;
    FAILWITH ;
}